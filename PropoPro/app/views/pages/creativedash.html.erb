<head>
  <style>
  body {
    text-align: center;
    font-family: 'Montserrat', sans-serif;
  }
    .proposals {
      text-align: left;
      position: relative;
      width: 50vw;
      height: 5vh;
      background: #E3EFF3;
      margin-bottom: 20px;
      margin: auto;
      border-radius: 5px;
    }

    .proposals a span {
      position:absolute;
      width:100%;
      height:100%;
      top:0;
      left: 0;
      z-index: 1;
    }

    .proposals:hover{
      opacity: .5;
    }

    a {
      color: #6E828A;
      text-decoration: none;
    }

    .left {
      padding: 10px;
      width: 50%;
    }


    .right {

    }

    h7 {
      font-size:10px;
      margin:0;
      padding:0;
    }

    h8 {
      color: white;
      background: rgba(50,205,50, 0.8);
      font-size:10px;
      border-radius: 10px;
      width: 5vw;
      text-align:center;
      margin-bottom: 5px;
    }

    h9 {
      color: white;
      background: rgba(242,40,22, 0.8);
      font-size:10px;
      border-radius: 10px;
      width: 5vw;
      text-align:center;
        margin-bottom: 5px;
    }

    h5 {
      font-size:15px;
      margin:0;
      padding:0;
    }

  </style>
</head><%= link_to "Sign Out", destroy_admin_session_path, method: "delete" %><br><br>

WELCOME TO YOUR DASHBOARD CREATIVE PERSON

<br><br>
Here are all your submitted proposals.<br><br>
<% @proposals.each do |proposal| %>
  <div class="proposals">
    <a href= <%= "#{proposal_path(proposal)}" %>>
      <span>
          <div class="left">
            <%= proposal.title.capitalize %> <br>
            <h7>Prepared for <%= proposal.email %></h7>
          </div>
          <!-- <div class="right">
           -->
              <!-- <% if proposal.proposal_accepted %>
              <h8>Accepted<br></h8>
              <%else%>
              <h9>Accepted<br></h9>
              <%end%>
              <% if proposal.invoice_paid %>
              <h8><%= number_to_currency(proposal.budget, precision: 0)%><br></h8>
              <%else%>
              <h9><%= number_to_currency(proposal.budget, precision: 0)%><br></h9>
              <%end%> -->
              <!--
            </div> -->
          </div>

      </span>
    </a>
  </div><br>
<% end %>
